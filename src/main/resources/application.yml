---
server:
  port: 8888
security:
  basic:
    enabled: true
  user:
    name: plural
    password: plural
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sokolinthesky/cloudconfig-wa-tolls
          search-paths:
            - 'station*'